import { UsernameMatcherDirective } from './username-matcher.directive';

describe('UsernameMatcherDirective', () => {
  it('should create an instance', () => {
    const directive = new UsernameMatcherDirective();
    expect(directive).toBeTruthy();
  });
});
