import { PwdMatcherDirective } from './pwd-matcher.directive';

describe('PwdMatcherDirective', () => {
  it('should create an instance', () => {
    const directive = new PwdMatcherDirective();
    expect(directive).toBeTruthy();
  });
});
